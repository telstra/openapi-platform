import React, { SFC } from 'react';
import { observer } from 'mobx-react';
// TODO: Add react-router's injected props
export const SettingsViewer: SFC<any> = observer(() => (
  <div>Some settings should go here</div>
));
